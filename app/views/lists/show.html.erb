

<div class="container">
  <div class="row">
    <div class="col-md-12 mt-3">
      <h1>Lists#show TEST</h1>
      <p>Find me in app/views/lists/show.html.erb</p>


        <div class="card p-3 mb-3">
          <table>
            <h2><%= @list.title %> ID: <%= @list.id  %> </h2>
              <tr class="row-border">
                <th>#:</th>
                <th>Item:</th>
                <th>Description:</th>
                <th>Price:</th>
                <th>Link:</th>
                <th>Update:</th>
                <th>Delete:</th>
              </tr>

            <% @list.items.each do |item| %>

              <tr class="row-border">
                <td><%= item.id %></td>
                <td><%= item.item_name %></td>
                <td><%= item.description %></td>
                <td>£<%= item.price %></td>
                <td><%= item.link %></td>
                <td><%= render "lists/partial/update_item", list: @list, item: item %></td>
                <td><%= link_to "Delete", list_item_path(@list, item), class: 'btn btn-outline-warning', method: :delete, data: { confirm: 'Are you sure that you want to delete this item?' }, remote: true %></td>
              </tr>
            <% end %>
          </table>
        </div>

      <%= render "lists/partial/add_item" %>

    </div>
  </div>
</div>
